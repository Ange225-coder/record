{% extends 'base.html.twig' %}

{% block title %}Inscrivez un hébergement sur Record.com{% endblock %}

{% block body %}
    <section>
        <h1>Quelles langues parlez-vous ?</h1>

        {{ form_start(languagesForm) }}

            <div>
                {{ form_label(languagesForm.languages) }}
                {{ form_widget(languagesForm.languages) }}
            </div>

            <button type="submit">Continuer</button>
        {{ form_end(languagesForm) }}
    </section>
{% endblock %}